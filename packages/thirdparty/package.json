{
  "name": "@reagentai/thirdparty",
  "version": "0.7.12",
  "description": "Thirdparty integrations for Reagent AI",
  "type": "module",
  "scripts": {
    "tsc:watch": "tsc --watch",
    "build": "tsc",
    "clean": "rm -rf node_modules && rm -rf dist",
    "prepublish": "rm -rf dist && pnpm build && cp package.json dist"
  },
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": true
  },
  "license": "MIT",
  "keywords": [],
  "dependencies": {
    "@e2b/code-interpreter": "^0.0.8",
    "@reagentai/react": "workspace:^",
    "@reagentai/reagent": "workspace:^",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.2",
    "typescript": "5.4.5"
  }
}
