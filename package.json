{
  "name": "@portal/reagent-workspace",
  "version": "0.1.0",
  "description": "Portal Cortex packages",
  "type": "module",
  "scripts": {
    "format": "pnpm prettier -w .",
    "demo-remix": "pnpm -r --filter @portal/reagent-remix run dev",
    "demo-nextjs": "pnpm -r --filter @portal/reagent-nextjs run dev",
    "publish": "pnpm -r publish --no-git-checks --access public --filter @reagentai/reagent --filter @reagentai/serve --filter @reagentai/react"
  },
  "keywords": [],
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
